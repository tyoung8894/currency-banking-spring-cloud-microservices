spring:
  application:
    name: limits-service
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active:
    - dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka  
    
    
    
